# Growth agent evaluation

Generated by hand from `scripts/test-growth-agent.ts <TICKER>` and, for the claude-cli MSFT row, a full live `scripts/run-report.ts` pipeline run (not the standalone smoke test). Agent: `.claude/agents/analysis/growth.md`, output: `growthDrivers` (`BulletItem[]`). Checks come from `lib/agents/grounding.ts`'s `checkBulletGrounding()`, same as risk.md.

| provider | model | ticker | zod | factId จริง | เลขตรง | items | retry | เวลา | note |
|---|---|---|---|---|---|---|---|---|---|
| groq | openai/gpt-oss-20b | 1773.HK | ✓ | ✓ | ✓ | 2 | 0 | ~3s | clean — "Strong Revenue and Earnings Growth Momentum", "Margin Expansion and Operating Leverage", both well-grounded, no overlap with riskFactors' debt-focused items for the same ticker |
| claude-cli | sonnet | MSFT | ✓ | ✗ | ✗ | 3 | 0 | 80.5s | 1 item ("Accelerating Revenue Growth Backed by Rising Forward Estimates") has a fabricated factId + 3 downstream unsupported-number flags — see below. Live full-pipeline run, 2026-08-20 |

## Real near-miss factId fabrication, verified against the DB

The MSFT item cited `cmt01xklw000zo0wkma9yeyzq` — doesn't exist. Facts sharing that id's exact 17-character prefix (`cmt01xklw000zo0wk`):

| cited (doesn't exist) | real fact | value |
|---|---|---|
| `cmt01xklw000zo0wkma9yeyzq` | `cmt01xklw000zo0wkhfpfxinj` (RevenueFromContractWithCustomerExcludingAssessedTax, 2026-06-30) | 331,839,000,000 |

The claim's own stated number ("331840000000") is a near-exact match to the real fact's value (331,839,000,000 — off only by rounding) — the *number* is correct, only the *citation* is fabricated. Because the real Revenue fact was never actually cited, that same number reads as unsupported too (a downstream consequence of the one fabrication, not a second independent problem — same mechanic documented in `docs/eval/risk.md`/`moat.md`). A second number in the same item ("130000000000") is also flagged; not independently traced, but likely another instance of the same missing-citation cascade rather than a distinct issue.

**This is now the third independent confirmed occurrence of this exact fabrication shape from claude-cli/sonnet** (same 17-character-prefix-correct, suffix-fabricated pattern each time):

1. `docs/eval/moat.md` — MSFT, `scale_economies`/`brand` items, standalone `test-moat-agent.ts` run
2. `docs/eval/risk.md` — 1773.HK, `riskFactors`, live full-pipeline `run-report.ts` run
3. This entry — MSFT, `growthDrivers`, live full-pipeline `run-report.ts` run

Three occurrences across three different sections and two different tickers, always the identical shape (correct prefix, fabricated suffix, always on a longer/denser prompt), is strong enough evidence to treat this as a systematic property of claude-cli/sonnet on this kind of task, not an occasional fluke. The grounding-check gate (now wired into `scripts/run-report.ts` — see `docs/eval/synthesis.md`'s "Full live pipeline run" entries) is doing exactly the job it exists for here: this MSFT report was correctly held back from the Obsidian export because of it.
