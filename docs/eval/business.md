# Business agent evaluation

Generated by hand from `scripts/test-business-agent.ts <TICKER>` (`AGENT_PROVIDER=claude-cli`, the current `.env` default — no all-provider sweep script like `eval-models.ts` exists for this agent, same as growth/risk/moat). Agent: `.claude/agents/analysis/business.md`, output: `businessSummary` (`ClaimItem[]`). Checks come from `lib/agents/grounding.ts`'s `checkClaimGrounding()` (see `scripts/test-grounding.ts`'s `checkClaimGrounding` section): **factId จริง** = every `supportingFactIds` entry exists in FinancialFact; **เลขตรง** = every financial-looking number in `claim` is within ±5% of some fact among that item's `supportingFactIds`.

Unlike valuation/risk/moat, this agent also needs `companyProfile` context (`Stock.description`/`sector`/`industry`, built by `lib/agents/context.ts`'s `buildCompanyProfileContext()`) — not every ticker has a `Stock` row, so the two test tickers here exercise both paths: MSFT has a cached description, 1773.HK does not.

| provider | model | ticker | zod | factId จริง | เลขตรง | paragraphs | retry | เวลา | note |
|---|---|---|---|---|---|---|---|---|---|
| claude-cli | sonnet | MSFT | ✓ | ✓ | ✓ | 3 | 0 | 37.0s | companyProfile present (Yahoo longBusinessSummary) |
| claude-cli | sonnet | 1773.HK | ✓ | ✓ | ✓ | 3 | 0 | 33.4s | no Stock row — no-description fallback path |

No grounding failures on either run.

## Manual review (checkClaimGrounding can't check these)

**1. MSFT — do the product/segment claims trace to `[companyProfile]`, not the model's own training-data memory of the ticker?** — yes: the three named segments (Productivity and Business Processes, Intelligent Cloud, More Personal Computing) and their named products (Microsoft 365, Teams, LinkedIn, Dynamics 365, Azure, SQL Server, Windows Server, Surface, Xbox, Bing/Copilot) all come from the `companyProfile` text passed in, not just plausible-sounding Microsoft trivia — the segment breakdown in particular isn't guessable from a generic "Microsoft makes software" prior, it has to have been read off the profile.

**2. 1773.HK — is the no-description fallback honest (states the gap) rather than guessing anyway?** — yes, and cleanly: the first paragraph opens with "ไม่มีข้อมูลเชิงพรรณนาธุรกิจ...จึงไม่สามารถระบุได้ว่าบริษัทดำเนินธุรกิจประเภทใด" (no business description available, so what the company actually does can't be determined) and explicitly scopes every subsequent claim to "ตัวเลขทางการเงินที่มีอยู่เท่านั้น" (financial figures only). The second paragraph repeats the caveat ("แม้จะไม่ทราบรายละเอียดว่าธุรกิจหลักของบริษัทคืออะไร") rather than dropping it after paragraph one. This is exactly the honest-gap behavior business.md rule #3 requires — the model never reaches for a plausible-sounding guess about what a Hong Kong–listed 1773 might do.

**3. Do the three paragraphs overlap or restate each other?** — no, on both tickers: each run cleanly splits into (a) what the business does / revenue scale, (b) profitability margins, (c) size/valuation or capital-return metrics, with no repeated factIds across paragraphs.

## Conclusion

Both tickers pass zod and grounding cleanly on the current default provider (claude-cli/sonnet). No fix needed this round — first clean pass for this agent, unlike risk.md's and synthesis.md's first attempts (both needed a fix after the initial eval). Worth revisiting with a second provider (gemini/groq) later, same caveat as growth/risk/moat: this doc only covers the `.env` default.
